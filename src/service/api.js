
import data from "../data"

export async function fetchUserData (url) {
    if (!url)
        return data;
    else {
        // fetch data from server with the given url
    }
}
export default fetchUserData